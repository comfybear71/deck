# Skidmarks Music Video — audit against the 2026-09-16 brief

Scope: Music video only. Save, Archive, Jack Ash lock, script logic.

**Honest limit up front.** This audit was done by reading the code and running the unit tests. No iPhone Safari session was available here, so every row below is a *code* verdict. The brief's own rule stands: it is not fixed until S1 and A1 pass on Stuart's phone. The "Phone check" column is what he has to do.

## Wipe switches (every place the live song is cleared)

| Function (`lib/skidmarks.ts`) | Called from | Verdict |
|---|---|---|
| `selectSkidmarksBand` sets `mp3: null` | `SkidmarksDetailSheet.handleSelectBand`, only after `archiveBeforeSwitch` returns true | OK |
| `createSkidmarksBand` sets `mp3: null` | `handleCreateBand`, same gate | OK |
| `removeSkidmarksBand` (active band) sets `mp3: null` | trash glyph on a band tile | Not gated by archive. Deleting the live band deletes its song. Left as is: deleting a band is an explicit destructive tap, not a save/switch path. Flagged. |
| `clearSkidmarksMp3` | the MP3 card's remove control | Explicit user action. OK |
| `resetSkidmarksSessionAfterArchive` | `archiveBeforeSwitch` (New / switch band / open a different song) only. **No longer called by the Archive button.** | OK |

## Results

### Save

| Test | Code verdict | What was wrong / what changed | Phone check |
|---|---|---|---|
| S1 kill Safari, reopen | PASS (code) | Mirror is written synchronously on every edit and the app now **boots from the mirror** before the first paint. Server copy reconciled after. Added a green "Saved ✓" line so "wait until the UI says saved" is actually possible; before this the UI said nothing when synced. | Required |
| S2 lock 10 min | PASS (code) | Same path as S1. `visibilitychange` flush on lock. | Required |
| S3 power off/on | PASS (code) | Same path. If the last edit never left the phone, the mirror carries an `unsynced` flag and wins over the server on reopen. | Required |
| S4 airplane mode edit | PASS (code) | Mirror written offline; `online` event and a 60 s retry loop push it. | Required |
| S5 forced failed save shows NOT SAVED, refresh shows last good project | **FAIL → fixed** | Known failure mode #3 was real: app booted on seed state; a tap before the server answered discarded the real row, then the next push had nothing "substantive-seen" to guard and PUT the seed state over the real song. Fix: `resolveSkidmarksHydrationWinner` (a real server row always beats a seed-only local state), boot from mirror, and **no PUT is sent until the load has settled** (`hydrationSettled`). | Required |
| S6 no `data:` on the wire | PASS, one gap closed | Band cover, member avatar and plate stills were already stripped. `scriptSequenceDraft.startingImageUrl` was not covered; now stripped too. | Optional (Safari Web Inspector) |

### Archive

| Test | Code verdict | Notes | Phone check |
|---|---|---|---|
| A1 Archive leaves desk, row appears, honest message | PASS (code) | Already true after #108. Message reworded to the brief's wording ("Archive copy saved… also still on your desk"). | Required |
| A2 Finished Songs open by default | PASS | `useState(true)` in the shelf. | — |
| A3 Open in editor restores the same song | PASS, order fixed | Snapshot is now downloaded **before** the current song is archived or the desk touched, so a bad download changes nothing. | Required |
| A4 orphan snapshot recovered to the list | PASS | `recoverOrphanedSnapshots` in the archive route runs on every list read and rewrites the index. | Optional |
| A5 New / switch archives first, clears only on success | PASS (code) | Unchanged gate; failure message now says the song is still there. Unchanged songs (already on the shelf) skip the upload instead of listing a duplicate. | Required |
| L3 Archive does not mean delete | **FAIL → fixed** | "Open in editor" deleted the shelf row on open, so the song's only copy became the live session. If the session save then failed, it looked gone. The row now stays. A song restored from the shelf is fingerprinted so leaving it again never duplicates the row. | Required |

### Jack Ash + render

| Test | Code verdict | Notes |
|---|---|---|
| J1 no zoom/orbit/pan on a Jack vocal | **Partial → fixed, then revised by Part 3** | Automatic default was already "Camera holds" for a locked vocalist, but a typed "zoom" note and a script "tracking shot" could both still ask for camera movement. Final state (see Part 3 below): the app never adds a camera move of its own anywhere; his typed text is sent as written, and the render card warns before the paid tap when his own motion or shot text moves the camera on a Jack vocal. |
| J2 frame never fully black | PASS (prompt) | `lockedCharacterVideoNote` already demands one visible anchor. No structural QA gate exists yet (see "Not done"). |
| J3 no second person | PASS (prompt) | Solo-shot sentence on every render plus negative cues. |
| J4 after 3 clips, new still of a new place | **FAIL → fixed** | Runner chained every clip with no reset (reverted to that on 2026-09-16). Now a locked vocalist chains inside a 3-clip block; clip 4 starts from a **freshly generated still of its own scene**, never the last frame and never the master photo. If that still fails, the run stops honestly and Resume generates it again. |
| J5 10 s part renders ~10 s | **FAIL → fixed** | Runner hard-coded 15 s for every clip. Now uses the part's own `(start - end)` span clamped to 5–15 s. |
| L1 script times = clip length | Fixed with J5 | |
| L4 render of Jack ≠ new actor | Prompt + graph | Refine strength 0.6, enhancer forced off, pad not crop. **Still true and not changed here:** both CFG guiders run at `cfg: 1`, so the negative prompt has no mathematical effect. Raising CFG on this distilled LTX graph is a render-quality change I could not test in this sandbox, so it is left alone and stated plainly. |

### Stop button (report item 15)

Read twice. `shouldStop` is checked at the top of every loop iteration, i.e. after the current render and its chain, before the next render starts. That is correct for "stop the next clip". It is now also checked before a scene-block still is generated (a paid call). I could not reproduce "next clip still started" in code; if it happens again, note whether the clip that started was the one already rendering when Stop was tapped (expected, already paid) or a genuinely later one.

## Not done (out of scope for this pass, listed so nobody "rediscovers" them)

- No after-render QA gate (is the last frame near-black / did a face appear). J2 relies on the prompt.
- No per-song spend cap or cost history.
- No checkpoint version list UI; Finished Songs rows are the checkpoints.
- Deleting the active band still deletes its live song without an archive first.
- Neon smoke test fails in this sandbox because the database URL is unreachable here. Pre-existing, unrelated.
- `app/layout.tsx` fails `tsc` on a missing Next generated type (`LayoutProps`). Pre-existing, unrelated.

## Part 3 — hidden prompt text (added same day)

Confirmed: `buildClipGenerationRequest` wrapped Stuart's shot and motion text in up to six app-written blocks, and a blank motion box on any non-Jack render silently sent "Slow cinematic push-in zoom". Stills did the same with a factory framing line.

| Test | Verdict | What changed |
|---|---|---|
| P1 Jack vocal, motion empty: no push-in/zoom/orbit/pan anywhere | **FAIL → fixed** | Blank motion on a Jack vocal now adds one line only: camera holds, he moves. The Jack video note was rewritten so it never names a camera move even to forbid one. The Vocal LTX lock's own trailing "Camera holds." was removed. |
| P2 typed motion sent as written, no factory line after | **FAIL → fixed** | His shot text is first, his motion second, always. Lock text follows. Nothing is appended after his motion except the lock and the no-text footer. |
| P3 Instrumental, motion empty: no automatic push-in | **FAIL → fixed** | The factory default is gone. Blank motion means no camera line. "Cinematic motion" was also dropped from the footer. |
| P4 lock text may stay; camera verbs only if he wrote them | **FAIL → fixed** | A locked character's Vocal render keeps shadow-face, neon-lips and solo-shot text. The solo-shot line no longer leaks onto non-Jack renders. Camera verbs in the sent prompt only ever come from his own text. |
| Stills | fixed | Instrumental stills no longer get "Wide dynamic … camera angle" or "Simple, clean single-subject". The vocal lip-sync framing (¾ angle, eyes away from the lens) stays because it is a Jack rule, not a camera move. |
| Preview | added | Each render card has a collapsed "Prompt LTX/H3/Grok will get" panel built by the same function the render uses, and a live motion counter against the 600 cap. |

**One deliberate change to the morning's J1 fix.** This morning a typed zoom on a Jack vocal was replaced with a static camera. Part 3 says user text wins, so it is now sent exactly as typed and an amber warning explains why that is risky. If you want the rewrite back, it is one line in `buildClipGenerationRequest`.

Server side: `lib/comfyCloud.ts` only appends Jack's negatives to the negative node and never touches the positive prompt; the generate-clip route sends the prompt through unchanged. No hidden text there.
